# Google Gemini API key (for MedGemma-style capabilities). Get one at https://aistudio.google.com/apikey
# For production with Vertex AI MedGemma, point the backend to your Vertex endpoint instead.
GEMINI_API_KEY=

# Optional: model name (default: gemini-2.0-flash)
# MEDGEMMA_MODEL=gemini-2.0-flash

# Optional: port (default: 4000)
# PORT=4000
